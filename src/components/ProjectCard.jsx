/*  
This .jsx (JavaScript XML) file will contain the JSX code for the ProjectCard component. 
The component will be a card that displays the project name, description, and the tech stack, along with a link
to the project's GitHub repository, a link to the live demo, and a button to view the project documentation. 
 */

import { Component } from "react";

export default class ProjectCard extends Component {
  render() {
    return <h1>ProjectCard</h1>;
  }
}
